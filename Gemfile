source :rubygems

# gemspec gems must be replicated here because there seems to be a bug
# when bundler tries to load the gemspec file, within the jar execution context.
gem 'rspec', '~> 2.8.0'
gem 'rake', '~> 0.9.2.2'
gem 'ruby-maven', '~> 3.0.3.0.28.5'
